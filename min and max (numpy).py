#it is not working here cuz its idle el badan fa45
import numpy as np
z=np.array([[1,2,3],[4,5,6]])
print(min(z))
#it will appear the min value for the whole array
print(min(z),axis=0)
#it will appear the min (n (number of coulms)) value of all rows in array
print(min(z),axis=1)
#it will appear the min (m (number of rows )) value of all colums in array

 # هتطلعهم في مضفوفة يعني
